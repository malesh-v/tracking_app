<%= form.label 'Assign to' %>
<%= collection_select(:ticket, :staff_member_id, StaffMember.all, :id,
                      :login, { include_blank: 'Unassigned' }, class: 'form-control') %>


<%= form.label :status %>
<%= collection_select(:ticket, :status_id, Status.all, :id, :name, { multiple: false }, class: 'form-control') %>